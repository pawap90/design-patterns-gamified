name: CI/CD
on:
  push:
    branches: [ main ]

  workflow_dispatch:

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
      
      - name: Build strategy-pattern-space-shooter
        uses: pawap90/design-patterns-gamified/.github/workflows/build-phaser.yml@main
        with:
          project_folder: strategy-pattern-space-shooter
        
      # Deploy latest contribution to GitHub pages
      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@4.1.5
        with:
          branch: gh-pages
          folder: _build
