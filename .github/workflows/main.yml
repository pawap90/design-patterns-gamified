name: CI/CD
on:
  push:
    branches: [ main ]

  workflow_dispatch:

jobs:
  build:
    uses: pawap90/design-patterns-gamified/.github/workflows/build-all.yml@main
 
  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
        
      # Deploy latest contribution to GitHub pages
      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@4.1.5
        with:
          branch: gh-pages
          folder: _build
